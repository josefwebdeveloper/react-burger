import styles from './order-feed.module.css';
import classNames from "classnames";
export const OrderFeed = () => {
    return (
        <section className={classNames(styles['order-feed'])}>
            <h2>order feed</h2>
        </section>
    );
};